// Tailwind CSS v4 — the PostCSS plugin lives in its own package and handles
// vendor prefixing internally (autoprefixer no longer required).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
